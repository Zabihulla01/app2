def risk_score(
    pf,
    sharpe,
    dd
):

    score = (
        (pf * 15)
        +
        (sharpe * 25)
        +
        ((100 - dd) * 0.25)
    )

    score = max(score, 0)

    score = min(score, 100)

    return round(score, 2)
